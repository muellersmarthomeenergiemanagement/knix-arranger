Launch the KNiX Arranger from source using:

```
python run.py
```

Run this command from the project root (`c:\Users\michimueller\AI\KNX Arranger Test3`).
The app is a PySide6 desktop application. Wait for the main window to appear, then interact with it to verify the change.
Do NOT launch anything from the `dist\` folder — those are stale PyInstaller builds.
